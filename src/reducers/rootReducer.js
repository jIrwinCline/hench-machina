const initState = {
    currentStep: 1,
    checkedItems: new Map(),
    handsOn: false,
    cost: "med",
    addInfo: "",
}

const rootReducer = (state, action) => {
    return state;
}

export default rootReducer